<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.embraceyourlife">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmbraceYourLife">
        <activity android:name=".GymStatsActivity"></activity>
        <activity android:name=".CreateExerciseActivity"></activity>
        <activity android:name=".DaySchedule">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GymActivity"></activity>
        <activity android:name=".TworzenieTreningu"></activity>
        <activity android:name=".CalendarActivity"></activity>

    </application>
</manifest>